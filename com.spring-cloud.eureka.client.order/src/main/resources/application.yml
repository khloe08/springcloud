spring:
  application:
    name: order

resilience4j:
  circuitbreaker:
    enabled: true  # Feign? Circuit Breaker ?? ???
    configs:
      default: # ?? Circuit Breaker ??
        registerHealthIndicator: true  # ?? ?? ???
        slidingWindowType: COUNT_BASED  # ???? ??? ?? (COUNT_BASED ?? TIME_BASED)
        slidingWindowSize: 5  # ?? 5?? ??? ???? Circuit Breaker ??? ??
        minimumNumberOfCalls: 5  # Circuit Breaker? ????? ?? ??? ?? ?? ??
        slowCallRateThreshold: 100  # ?? ?? ?? ?? (100%? ?? ?? ??? ??)
        slowCallDurationThreshold: 60000  # ?? ??? ???? ?? ?? (60?, ??: ???)
        failureRateThreshold: 40  # ?? ??? 50% ???? Circuit Breaker OPEN ??? ??
        permittedNumberOfCallsInHalfOpenState: 3  # HALF-OPEN ???? ??? ?? ?? ?
        waitDurationInOpenState: 20s  # OPEN ???? HALF-OPEN?? ?????? ?? ?? (20?)

management:
  endpoints:
    web:
      exposure:
        include: prometheus
  prometheus:
    metrics:
      export:
        enabled: true

server:
  port: 19091

eureka:
  client:
    service-url:
      defaultZone: http://localhost:19090/eureka/
